services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    command: uvicorn server:app --host 0.0.0.0 --port 8000

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend